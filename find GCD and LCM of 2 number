#include <stdio.h>

int main()
{
  int n1,n2,p;
  printf("enter two number:");
  scanf("%d%d",&n1,&n2);
   p=n1*n2;
   while(n1>0 && n2>0)
   {
       if(n1>n2)
          n1=n1-n2;
        else 
          n2=n2-n1;
    }
   if(n1==0)
  {
  printf("the Gcd of 2 number is:%d \n",n2);
  printf("LCM of 2 number is:%d",p/n2);
  }else {
      printf("the Gcd of 2 number is %d \n",n1);
        printf("LCM of 2 number is:%d",p/n1);
  }
  
  return 0;
}
