#include <stdio.h>

int main() {
    int n,i;
    float sum=0.0;
    printf("enter the elemnt of series upto:");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        sum = sum +(1.0/i);
    }
    
     printf("sum of the series is %f",sum);
    
    return 0;
}
